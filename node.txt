-inline: trái - phải

-block: trên - dưới

leading: khoảng cách các dòng

tracking: khoảng cách các chữ

justify: chiều ngang

capitalize: viết hoa chữ đầu

transparent: trong suốt

xl:justify-start => căn theo lề trái (nếu vượt qua breakpoint)

md: medium => màn hình vừa

defer: chạy theo thứ tự

overflow: kiểm soát tràn màn hình (container)

loop: lặp lại

inset: bám sát các cạnh theo container

order-first: chỉ định thứ tự hiển thị phần tử trong 1 container (flex/grid) 

leading-loose sẽ làm cho line-height là 32px.

bg-[url(/public/assets/...)]: link ảnh